Flow:
User (Browser)
   ↓
S3 Static Website (HTML + JS)
   ↓
API Gateway
   ↓
Lambda Functions
   ↓
DynamoDB + S3 (order notes)

🏗 MODULE 1 – ORDERING THE BEVERAGES
STEP 1: Create DynamoDB Tables
Orders Table
DIY Table
Stocks Table: Pre-populate Stocks table with all ingredient codes and 0.
WR, IC, MK, FM, SR, ES, CP, VI, WY

STEP 2: Create S3 Bucket (Static Website)
STEP 3: UI (Same behavior as sample)
STEP 4: Lambda – Order Processing
STEP 5: API Gateway
Create REST API: POST /order
→ Lambda: order_lambda

🏗 Module 2 in logic in lambda

🏗 MODULE 3(UI is optional if mention)
Coffee Shop Owner
        ↓
API Gateway
   ├── GET /ordersummary
   ├── GET /salessummary
   └── GET /inventory
        ↓
     Lambda Functions
        ↓
     DynamoDB Tables

🔹 STEP 1 – Lambda: orderSummaryLambda
🔹 STEP 2 – Lambda: salesSummaryLambda
🔹 STEP 3 – Lambda: inventoryLambda
🔹 STEP 4 – API Gateway Configuration
| Method | Route         | Lambda             |
| ------ | ------------- | ------------------ |
| GET    | /ordersummary | orderSummaryLambda |
| GET    | /salessummary | salesSummaryLambda |
| GET    | /inventory    | inventoryLambda    |

